<div class="activity-list">
    <div class="row">
        {% for activity in collection %}
            <div class="col-12 pt-1 pb-2">
                <div class="block activity-list-item">
                    {% set activityTemplate = "BugTrackerBundle:Activity/"
                    ~ (activity.entity)
                    ~ ":"
                    ~ (activity.type)
                    ~ ".html.twig" %}
                    {{ include(activityTemplate, {activity: activity}) }}
                </div>
            </div>
        {% endfor %}
    </div>
</div>